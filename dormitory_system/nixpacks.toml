# ============================================================
# 注意: Railway 现在使用 Dockerfile 构建（见 railway.json）
# 此文件作废，保留仅作参考
# ============================================================

[providers]
providers = ["python"]

[phases.setup]
nixPkgs = ["mysql_dev", "pkg-config", "openssl", "zlib", "gcc"]
